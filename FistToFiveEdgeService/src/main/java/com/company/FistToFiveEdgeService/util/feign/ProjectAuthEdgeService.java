package com.company.FistToFiveEdgeService.util.feign;

import org.springframework.cloud.openfeign.FeignClient;

@FeignClient(name = "account-service")
public interface ProjectAuthEdgeService {

//    add routes here?
}
